import { SafeAreaView, Text } from 'react-native'

import { HomeStackNavProps } from '@navigation'
import React from 'react'

export const AdminScreen: React.FC<HomeStackNavProps<'AdminScreen'>> = ({}) => {
  return (
    <SafeAreaView>
      <Text>sas</Text>
    </SafeAreaView>
  )
}
